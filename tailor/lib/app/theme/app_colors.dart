import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary Navy Blue (matching design)
  static const Color primary = Color(0xFF1B2A6B);
  static const Color primaryDark = Color(0xFF0F1A4E);
  static const Color primaryLight = Color(0xFF2D3E8F);

  // Accent / Secondary
  static const Color accent = Color(0xFFC9813A);
  static const Color accentLight = Color(0xFFF5E6D3);

  // Background
  static const Color background = Color(0xFFF7F8FC);
  static const Color cardBg = Colors.white;
  static const Color surface = Color(0xFFF0F2F8);

  // Text
  static const Color textPrimary = Color(0xFF1A1A2E);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textMuted = Color(0xFF9CA3AF);
  static const Color textOnPrimary = Colors.white;
  static const Color textOnAccent = Colors.white;

  // UI
  static const Color divider = Color(0xFFE5E7EB);
  static const Color inputBorder = Color(0xFFD1D5DB);

  // Status
  static const Color success = Color(0xFF10B981);
  static const Color error = Color(0xFFEF4444);
  static const Color warning = Color(0xFFF59E0B);
  static const Color info = Color(0xFF3B82F6);

  // Misc
  static const Color gold = Color(0xFFC9A96E);
  static const Color starColor = Color(0xFFFFB800);

  // Heart / Like
  static const Color heartRed = Color(0xFFEF4444);
}
